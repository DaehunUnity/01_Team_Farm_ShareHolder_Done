using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class touchInput : MonoBehaviour
{
    public float valX { get; protected set; }
    public float valY { get; protected set; }

    public bool Ontouch { get; protected set; }
    public bool Onvalue { get; protected set; }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
